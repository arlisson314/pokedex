import styled from 'styled-components';

const Container = styled.div`
  >input {
      padding-left: 1ch;
      margin-top: -1.6em;
      outline: none;
      height: 2em;
      border-radius: 5px;
      border: none;
    }
`;
export default Container;
